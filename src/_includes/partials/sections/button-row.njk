<div class="container" style="padding-top:var(--space-6);padding-bottom:var(--space-6);display:flex;gap:var(--space-4);flex-wrap:wrap;">
  {% for btn in section.buttons %}
  <a href="{{ btn.url }}" class="btn btn--{{ btn.style or 'primary' }}">{{ btn.label }}</a>
  {% endfor %}
</div>
