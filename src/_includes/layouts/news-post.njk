---
layout: layouts/base.njk
mainClass: page-news-post
---

<article class="news-post">

  <header class="news-post-header">
    <div class="container">
      {% set breadcrumbs = [{url: "/news/", label: "News"}, {label: title}] %}
      {% include "partials/breadcrumb.njk" %}
      <div class="news-post-meta">
        <span class="badge {{ category | badgeClass }}">{{ category | categoryName }}</span>
        <time datetime="{{ date | dateISO }}" class="news-post-date">
          {% icon "calendar", 16 %}
          {{ date | dateFull }}
        </time>
      </div>
      <h1 class="news-post-title">{{ title }}</h1>
      {% if excerpt %}
        <p class="news-post-excerpt">{{ excerpt }}</p>
      {% endif %}
    </div>
  </header>

  <div class="news-post-body">
    <div class="container">
      <div class="news-post-content prose">
        {% if image %}
        <figure class="news-post-figure">
          <img src="{{ image }}" alt="{{ imageAlt or '' }}" loading="eager" decoding="async">
        </figure>
        {% endif %}
        {{ content | safe }}
      </div>
      <div class="news-post-footer">
        <a href="/news/" class="btn btn--outline news-post-back">
          {% icon "arrow-left", 18 %}
          Back to News
        </a>
      </div>
    </div>
  </div>

</article>